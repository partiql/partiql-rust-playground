import { IonEvent } from "../Ion";
export declare class EventStreamError extends Error {
    type: string;
    index: number;
    eventstream: IonEvent[];
    constructor(type: string, message: string, index: number, eventstream: IonEvent[]);
}
